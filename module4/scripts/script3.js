// Use the different namespaces to protect the variables and function in diff scripts
// mayaGreeter.sayHello();
// bettyGreeter.sayHi();

// Immediately Invoked Function Expressions (IIFEs)
console.log('Immediately Invoked Function Expressions (IIFEs)');

// A function is invoked via ()
// The first set of () produce the expression ie the function object
// Second set invoke
// (function () {
//   console.log('Hi Maya');
// })();

mayaGreeter.sayHello();
bettyGreeter.sayHi();